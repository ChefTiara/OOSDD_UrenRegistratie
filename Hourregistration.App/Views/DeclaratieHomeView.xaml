<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Hourregistration.App.Views.DeclaratieHomeView"
             Title="Urenregistratie">

    <Grid Padding="20" RowDefinitions="Auto, Auto, *" ColumnDefinitions="3*, 1*">

        <!-- Title -->
        <Label Grid.Row="0" Grid.Column="0"
               Text="Welkom bij de urenregistratie"
               FontSize="22"
               FontAttributes="Bold" />

        <!-- New declaration button top right -->
        <Button Grid.Row="0" Grid.Column="1"
                Text="Nieuwe declaratie aanmaken"
                BackgroundColor="#9BD4A6"
                TextColor="Black"
                CornerRadius="6"
                Padding="14,10"
                HorizontalOptions="End"
                VerticalOptions="Center"
                Clicked="OnNieuweDeclaratieClicked" />

        <!-- Subtitle -->
        <Label Grid.Row="1" Grid.Column="0"
               Text="Wat wilt u doen?"
               FontSize="14"
               TextColor="Gray"
               Margin="0,5,0,15" />

        <!-- Draft box (big box left) -->
        <Border 
            Grid.Row="2" Grid.Column="0"
            Stroke="Black"
            StrokeThickness="1"
            BackgroundColor="White"
            Padding="15"
            HeightRequest="350">
            
            <!-- Contents draft box -->
            <VerticalStackLayout>
                <Label Text="Concepten"
                       FontAttributes="Bold"
                       FontSize="16" />

                <Label Text="U heeft geen concepten"
                       FontSize="13"
                       TextColor="Gray"
                       Margin="0,10,0,0"/>
            </VerticalStackLayout>
        </Border>

        <!-- Empty right column -->
        <StackLayout Grid.Row="2" Grid.Column="1" />

    </Grid>
</ContentPage>
